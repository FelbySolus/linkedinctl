#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
workspace_root="$(cd "$script_dir/.." && pwd)"
cd "$workspace_root"
npm run build >/dev/null
exec node browser/dist/main.js "$@"
